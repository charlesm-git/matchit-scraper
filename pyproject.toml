[tool.poetry]
name = "scraper"
version = "0.1.0"
description = ""
authors = ["cmareau-git"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"
requests = "^2.32.5"
sqlalchemy = "^2.0.45"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
